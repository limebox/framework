<deploy>
    <configuration>
        <!-- This is using a Temp folder for the wildcard, Instead of a wildcard, add a new line for each Customization outside of the Temp folder. -->
        <path>~/AccountConfiguration/Temp/*</path>
    </configuration>
    <files>
        <!-- This is a dummy script file. Instead of a wildcard, add a new line for each File. A better explanation is in this file -->
        <path>~/FileCabinet/SuiteScripts/sdf_ignore</path>
    </files>
    <objects>
        <!-- This is using a Temp folder for the wildcard, Instead of a wildcard, add a new line for each Object outside of the Temp folder. -->
        <path>~/Objects/Temp/*</path>
    </objects>
</deploy>